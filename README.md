# Image Editor App

### Editor is allowing user to upload an image. user can write some text over the image. user can change the font size, font weight and color of the text written over the image.

User can change the theme of the image and can apply filter over the image , selecting from the dropdown.

### Basic authentication is applied to the web app, user can authenticate using that.

Credentials :

username: user@example.com

password: password
